# Ruminote 如觅书摘 · KOReader 插件 v1.0.1

## 修复
- 修复书名读取不稳定导致同一段划线重复同步的问题。KOReader 元数据里的书名有时读不到，
  过去会落成「未知书籍」，使同一本书在不同时机算出不同的书籍指纹，同一条书摘被当成新书摘重复入库。
  现在书名读不到时改用文件名（去扩展名）兜底，保证同一本书始终对应同一份书摘，不再重复。

---

# Ruminote 如觅书摘 · KOReader 插件 v1.0.0

首个正式发布版本。

## 功能
- 扫描当前书籍的高亮/笔记并加入本地队列，联网后一键同步到 Rumi书摘
- 离线队列持久化，断网自动保留，下次同步补传
- 内容指纹幂等，同一条书摘重复同步不会重复入库
- 六位配对码绑定 Rumi书摘账号（在小程序「我的 → 我的设备 → 绑定新设备」生成）
- 兼容多版本 KOReader 的 annotations 数据结构

## 安装
1. 下载 `ruminote.koplugin-v1.0.0.zip` 并解压，得到 `ruminote.koplugin/` 文件夹
2. 放入 KOReader 的 `plugins/` 目录（最终路径应为 `plugins/ruminote.koplugin/main.lua`，注意不要多套一层目录）
3. 完全退出并重新打开 KOReader
4. 打开任意书籍 → 顶部菜单 → 工具 → 「Ruminote 如觅书摘」

## 使用
1. 在 Rumi书摘小程序生成配对码
2. 插件菜单 → 「绑定账号」输入配对码
3. 阅读时划线 → 插件菜单 → 「立即同步」（或「扫描本书书摘」先入队）

## 已知限制
- 仅同步新增书摘；在 KOReader 中删除/修改的高亮不会同步到云端
- 同步需要设备联网（Wi-Fi）

## 校验
- 平台：KOReader 2024.x（Android 真机验证通过）
- License：GPL-3.0
